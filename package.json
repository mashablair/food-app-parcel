{
  "name": "food-app-parcel",
  "version": "1.0.0",
  "description": "Find recipes based on the ingredients you already have",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url https://mashablair.github.io/food-app-parcel/.",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maria Blair",
  "license": "ISC",
  "dependencies": {
    "@popperjs/core": "^2.5.3",
    "bootstrap": "^5.0.0-alpha2",
    "parcel-bundler": "^1.12.4"
  },
  "devDependencies": {
    "cypress": "^5.3.0",
    "gh-pages": "^3.1.0"
  }
}
